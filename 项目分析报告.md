# 小卡（open-ai-canvas）项目分析报告与部署使用指南

> 面向小白的保姆级教程：看懂项目是什么 → 怎么装 → 怎么部署 → 怎么用。
> 仓库地址：https://github.com/xiaoka6688/open-ai-canvas（上游：ddcat-ai/open-ai-canvas）
> 分析版本：v1.5.4 ｜ 分析日期：2026-09-19 ｜ 本地部署位置：`d:\openai-canvas\open-ai-canvas`

---

## 一、项目是什么

**小卡（Xiaoka）** 是一个开源的 **AI 影视与短剧创作工作台**，一句话概括它的定位：

> 让一个故事，从文字走向银幕。

你可以把它理解为"AI 时代的影视制作流水线"：

- 用**自由画布**（类似无限白板）组织创作素材；
- 用**结构化工作流**管理剧本、角色、场景、分镜；
- 通过**统一的任务系统**调用 AI 生成图片、视频、音频和文本；
- 最后用**时间线剪辑器**把分镜片段拼成成片导出。

| 项目信息 | 内容 |
| --- | --- |
| 开源协议 | MIT |
| 技术来源 | 基于 basketikun/infinite-canvas 早期版本二次开发 |
| 当前版本 | v1.5.4（仍在快速迭代） |
| 适用场景 | 个人、本地或可信环境部署（**不建议未加安全配置直接暴露公网**） |
| 在线演示 | https://ddcat.pronhubcn.com （账号 test / 密码 test123456） |

---

## 二、核心功能一览

| 模块 | 能力说明 |
| --- | --- |
| 自由画布 | 节点、连线、框选、缩放、小地图、撤销重做、导入导出、只读分享、画布外观主题、自动整理节点 |
| 影视创作工作流 | 剧本、角色、场景、风格板、参考素材、结构化分镜脚本、3D 导演台 |
| 多媒体生成 | 文本/图片/视频/音频任务；支持参考图、首尾帧、运镜、续写、局部修改、批量生成、提示词 AI 优化器 |
| 任务与素材管理 | 异步任务队列、进度与日志、取消/重试、素材库、跨设备登录同步 |
| 时间线剪辑 | 片段编排、拆分、修剪、字幕转写（whisper.cpp）、服务端 ffmpeg 成片导出、AI 编辑助手 |
| 云端 Agent | 持久化对话、画布摘要、审批式画布写入、个人记忆、Skills 快照、流式事件回放 |
| 管理后台 | 系统渠道、逻辑模型、积分计费、支付插件（微信/支付宝/虎皮椒）、功能开关、站点外观、皮肤主题 |
| 其他 | 章节资产提取、批量创作表、版本记录（防覆盖）、用户诊断包导出、Eagle 外部素材库接入 |

---

## 三、系统架构（一张图看懂）

```
浏览器（web/  前端）
  ├─ React 19 + Vite + TypeScript 工作区界面
  ├─ Zustand / localForage  本地状态与降级缓存
  └─ 登录态 Cookie、业务 API、SSE 流式事件
          │  （/api 请求）
          ▼
后端（backend/  Go 1.25 + Gin + GORM）
  ├─ handler 层：HTTP 入参与鉴权
  ├─ service/app 层：业务编排（任务调度、权限、渠道中转）
  ├─ repository/model 层：数据持久化
  ├─ 异步任务 Worker（生成任务队列）
  └─ provider/outbound：对接外部 AI 模型渠道（SSRF 防护）
          │
          ├── SQLite（本地单机默认）或 PostgreSQL + Redis（生产部署）
          └── 资源存储：本地数据目录 / 阿里云 OSS / 腾讯云 COS
```

**三套可独立运行的单元：**

| 单元 | 目录 | 技术栈 | 入口 |
| --- | --- | --- | --- |
| 前端 | `web/` | Vite、React 19、Ant Design、Tailwind、Zustand、tldraw/leafer 画布 | `web/src/application.tsx` |
| 后端 | `backend/` | Go 1.25、Gin、GORM、SQLite/PostgreSQL | `backend/cmd/server/main.go` |
| 文档站 | `docs/` | Next.js、Fumadocs、MDX | `docs/content/docs/` |

---

## 四、目录结构速查

```
open-ai-canvas/
├── backend/                  # Go 后端
│   ├── cmd/server/           # 服务启动入口（main.go）
│   ├── internal/
│   │   ├── handler/          # HTTP 接口层
│   │   ├── service/          # 稳定导入面（别名层）
│   │   ├── app/              # 业务组合根（任务调度、渠道、计费等）
│   │   ├── repository/       # 数据库查询层
│   │   ├── model/            # 数据模型
│   │   ├── database/         # 连接与迁移
│   │   ├── auth/ prompts/ skills/ canvas/   # 认证、提示词、技能库、画布域
│   │   ├── platform/ kernel/ outbound/      # 运行时、错误码、出站防护
│   │   └── provider/         # 模型供应商协议实现
│   └── payment-plugins/      # 支付插件适配
├── web/                      # React 前端
│   └── src/
│       ├── pages/            # 路由页面（首页/画布/任务/素材/后台等）
│       ├── components/canvas/ # 画布节点与工具组件
│       ├── lib/canvas/       # 画布算法（布局、选择、连线）
│       ├── services/api/     # 统一 API 调用层（request.ts）
│       ├── stores/           # Zustand 状态
│       └── lib/plugins/      # 插件系统（提示词优化器等）
├── docs/                     # Fumadocs 文档站（快速开始/功能清单/代码地图）
├── plugin-packages/          # 支付插件包（微信/支付宝/虎皮椒）
├── scripts/                  # 启动与部署脚本（start-local.ps1 等）
├── docker-compose*.yml       # 各场景 Docker 编排
├── Dockerfile                # 前端镜像（Bun 构建 + Nginx 托管）
├── backend/Dockerfile        # 后端镜像（Go CGO 编译）
└── .env.example              # 环境变量样例
```

---

## 五、安装前准备（环境要求）

| 工具 | 版本要求 | 用途 | 下载地址 |
| --- | --- | --- | --- |
| Git | 任意较新版本 | 拉取代码 | https://git-scm.com/ |
| Bun | ≥ 1.3.9 | 前端依赖与开发服务器 | https://bun.sh/ （Windows 用 PowerShell 安装） |
| Go | ≥ 1.25 | 后端编译运行 | https://go.dev/ |
| Docker Desktop | 任意较新版本 | 容器化部署（方式 C） | https://www.docker.com/ |
| ffmpeg | 可选 | 时间线成片导出 | https://ffmpeg.org/ |
| whisper.cpp | 可选 | 时间线字幕转写 | 见 `docs/content/docs/backend/local-development.mdx` |

**Windows 安装 Bun 命令：**

```powershell
powershell -c "irm bun.sh/install.ps1 | iex"
```

**验证安装是否成功：**

```powershell
git --version
bun --version
go version
docker --version
```

---

## 六、部署方式（三选一）

> 本地部署目录约定：`d:\openai-canvas\open-ai-canvas`（下文用 `仓库根目录` 指代）

### 第 0 步：获取代码（所有方式通用）

```powershell
cd d:\openai-canvas
git clone https://github.com/xiaoka6688/open-ai-canvas.git
cd open-ai-canvas
```

---

### 方式 A：Windows 一键脚本启动（开发模式，推荐小白首选）

在仓库根目录执行：

```powershell
.\scripts\start-local.ps1
```

脚本自动完成：

1. 使用 `.local/project-workbench-debug` 作为后端 SQLite 数据目录；
2. 创建 `.local/cache/go-build`、`.local/cache/go-mod` 编译缓存；
3. `web/node_modules` 不存在时自动执行 `bun install --frozen-lockfile`；
4. 打开两个 PowerShell 窗口分别运行后端（8080）和前端（3000）；
5. 前端 `/api` 自动代理到 `http://127.0.0.1:8080`。

若执行策略拦截脚本：

```powershell
Set-ExecutionPolicy -Scope Process Bypass
.\scripts\start-local.ps1
```

> **Windows 下的关键坑（重要）**：后端 SQLite 依赖 `go-sqlite3`，需要 **CGO + C 编译器（gcc）**。
> 如果报错 `Binary was compiled with 'CGO_ENABLED=0', go-sqlite3 requires cgo to work`，
> 说明本机缺少 gcc。解决办法二选一：
> - 安装 MinGW-w64 / WinLibs 的 gcc 并加入 PATH；
> - **改用方式 C（Docker 部署）**，容器内自带 gcc，完全避开此问题（本次部署即采用此方式）。

---

### 方式 B：手动分终端启动（开发模式）

```powershell
# ① 创建本地数据与缓存目录（在仓库根目录）
mkdir .local\project-workbench-debug, .local\cache\go-build, .local\cache\go-mod

# ② 终端一：启动后端
cd backend
$env:CANVAS_BACKEND_ADDR = '127.0.0.1:8080'
$env:CANVAS_BACKEND_DATA_DIR = '..\.local\project-workbench-debug'
$env:GOCACHE = '..\.local\cache\go-build'
$env:GOMODCACHE = '..\.local\cache\go-mod'
go run ./cmd/server

# ③ 终端二：启动前端
cd web
bun install --frozen-lockfile
bun run dev
```

打开 http://localhost:3000 即可使用。后端健康检查：http://127.0.0.1:8080/api/health

> 注意：`web/` 只用 Bun + `bun.lock`，**不要**用 pnpm/npm 往同一套 node_modules 里再装。

---

### 方式 C：Docker 本地构建部署（推荐用于长期使用）★ 本次实际采用的部署方式

前提：Docker Desktop 已启动（守护进程运行中）。

```powershell
cd d:\openai-canvas\open-ai-canvas
docker compose -f docker-compose.local.yml up -d --build
```

该命令会：

- 构建 `open-ai-canvas-backend:local` 镜像：golang:1.25-alpine 内安装 build-base（含 gcc），CGO 编译 sqlite 驱动，走 `goproxy.cn` 国内代理加速；
- 构建 `open-ai-canvas-web:local` 镜像：oven/bun:1.3.13 执行 `bun install` + `vite build`，产物交给 nginx:1.27-alpine 托管并反代 `/api`；
- 启动两个容器，仅对外暴露前端 `3000` 端口（后端 8080 留在 Compose 内部网络）。

**常用运维命令：**

```powershell
# 查看容器状态
docker compose -f docker-compose.local.yml ps

# 查看后端日志
docker compose -f docker-compose.local.yml logs -f backend

# 停止 / 启动 / 重建
docker compose -f docker-compose.local.yml down
docker compose -f docker-compose.local.yml up -d
docker compose -f docker-compose.local.yml up -d --build   # 代码更新后重建
```

> 端口冲突时：在 `.env` 中设置 `CANVAS_WEB_HOST_PORT` 和 `CANVAS_BACKEND_HOST_PORT` 覆盖默认端口。

**其他 Compose 文件的区别（选读）：**

| 文件 | 用途 |
| --- | --- |
| `docker-compose.dev.yml` | 源码热更新开发（改代码自动生效） |
| `docker-compose.local.yml` | 本地构建 release 镜像并运行（本指南采用） |
| `docker-compose.deploy.yml` | 生产部署（PostgreSQL + Redis + backend + web） |
| `docker-compose.build.yml` | 叠加在 deploy 上做源码构建 |

---

### 方式 D：Linux 云服务器一键部署（选读）

```bash
# 源码构建（推荐）
curl -fsSL https://raw.githubusercontent.com/ddcat-ai/open-ai-canvas/main/scripts/install-server.sh | sudo bash

# 或使用 GHCR 镜像（不需要源码）
curl -fsSL https://raw.githubusercontent.com/ddcat-ai/open-ai-canvas/main/scripts/install-server-image.sh | sudo bash
```

脚本自动安装 Docker、生成受保护的 `.env`（含随机 PostgreSQL 密码），启动全套服务。默认访问 `http://服务器IP:3000`。

---

## 七、首次使用配置（必读）

1. **访问系统**：浏览器打开 http://localhost:3000
2. **注册管理员**：首次注册的账号即为管理员（本地部署可通过环境变量 `CANVAS_REGISTRATION_ENABLED=false` 关闭公开注册）
3. **配置模型渠道**（核心！不配置无法生成任何内容）：
   - 进入 **管理后台**，找到「渠道」设置；
   - 添加你的 AI 模型渠道（如 OpenAI 兼容中转、阿里云百炼、火山引擎等），填入 API 地址和密钥；
   - 配置逻辑模型与价格档（本地自用可不配积分计费）。
4. **开始创作**：回到工作台首页，新建画布或进入创作工作台。

> 用户 API Key 保存在浏览器本地；任务创建时提交给自部署后端中转。只在可信部署和 HTTPS 链路中使用真实密钥。

---

## 八、功能使用指南（小白上手路径）

### 8.1 自由画布（核心操作台）

- **新建画布**：首页 → 画布列表 → 新建。空画布中央有"加号"起始框，点击即可添加节点。
- **添加节点**：底部工具栏"+"菜单，可添加文本、绘图、分镜、文件夹、图片、视频、音频、工作流节点，也可直接上传文件。
- **基础操作**：
  - 拖动节点摆放位置；节点间拉线建立引用关系（如"参考图 → 生成节点"）；
  - 空白处拖动 = 框选多个节点；按住空格/中键拖动 = 平移画布；
  - 滚轮缩放；底部工具栏可切换「区域选择」和「抓手工具」；
  - `Ctrl/Cmd + Z` 撤销，`Ctrl/Cmd + S` 保存。
- **智能整理**：底部工具栏「自动整理节点」一键把散乱节点按流程/网格排好。
- **画布外观**：底部工具栏「画布外观」可切换浅色/深色/自定义主题和网格样式。
- **版本记录**：右侧面板按日期展开历史版本，可预览、下载、恢复；防止旧缓存覆盖云端新内容。

### 8.2 生成媒体内容

1. 在画布添加图片/视频/音频/文本节点；
2. 点击节点打开**提示词面板**，输入描述；点击「优化」可用 AI 提示词优化器润色；
3. 选择模型（从后台配置的渠道中选择）；
4. 提交后任务进入异步队列，在「任务中心」查看进度、日志，支持取消/重试；
5. 生成结果自动回填节点，可继续连线作为下一个任务的参考图/首尾帧。

### 8.3 短剧工作流（结构化生产）

- 新建项目 → 选择短剧流水线；
- **剧本**：录入或生成剧本 →「章节资产提取」自动抽取角色、场景、道具；
- **角色/场景**：生成角色卡、三视图、场景图、风格板；
- **分镜**：结构化分镜脚本（支持 AI 规划与修复）→ 分镜首帧 → 分镜视频；
- **3D 导演台**：可视化调整镜头角度。

### 8.4 剪辑成片（时间线编辑器）

入口：项目详情 →「制作流程 → 剪辑成片」，或直接访问 `/projects/:id/editor`。

- 轨道操作：添加、拆分、移动、修剪片段（200 层历史，可撤销重做）；
- **字幕转写**：需本地 whisper.cpp 服务（环境变量 `CANVAS_WHISPER_BASE_URL`，未配置会明确报错）；
- **成片导出**：由服务端 ffmpeg 渲染（`CANVAS_FFMPEG_PATH`，留空则在 PATH 查找）；
- **AI 编辑助手**：用自然语言描述剪辑意图，自动解析为命令序列执行。

### 8.5 素材库与技能库

- **素材库**：所有生成的图片/视频/音频自动入库，支持标签、引用校验（有业务引用的素材不会被误删）；
- **技能库**：内置技能种子 + GitHub/ZIP 包导入，供 Agent 和创作流程调用。

### 8.6 云端 Agent（AI 助手）

- 画布内浮窗对话，支持移动/调整大小；
- 可执行画布写入（需审批）、媒体生成（受预算控制）；
- 「设置 → Agent 记忆」管理个人记忆（待审批准制）；
- 支持 `plan_update` 待办清单、`ask_user` 选项确认。

### 8.7 管理后台（管理员）

| 入口 | 功能 |
| --- | --- |
| 渠道管理 | 系统渠道、模型列表、排序、复制渠道 |
| 模型与计费 | 逻辑模型、价格档（售价/成本价）、视频 Token 报价 |
| 用户管理 | 用户列表、权限、用量 |
| 支付与对账 | 订单筛选、CSV 导出、每日对账（需先在插件中心启用支付插件） |
| 站点及外观 | 品牌名、Logo、登录页、SEO、ICP 备案、皮肤主题（内置 4 套可扩展） |
| 系统配置 | 功能开关、对象存储、响应拦截、注册限流策略 |

---

## 九、环境变量速查（.env.example 精华）

| 变量 | 默认值 | 说明 |
| --- | --- | --- |
| `CANVAS_BACKEND_ADDR` | `:8080` | 后端监听地址 |
| `CANVAS_BACKEND_DATA_DIR` | `data` | 数据目录（SQLite 数据库、上传文件） |
| `CANVAS_DATABASE_DRIVER` | `sqlite` | 数据库驱动（`sqlite` 或 `postgres`） |
| `DATABASE_URL` | 空 | PostgreSQL 连接串（postgres 模式必填） |
| `CANVAS_CORS_ORIGINS` | `http://localhost:3000` | 允许的跨域来源（公网务必精确设置，不要用 `*`） |
| `CANVAS_REGISTRATION_ENABLED` | `false` | 是否开放注册 |
| `CANVAS_AUTO_MIGRATE` | `true` | 启动时自动迁移数据库 |
| `CANVAS_WORKER_CONCURRENCY` | `3` | 任务 Worker 并发数 |
| `CANVAS_WHISPER_BASE_URL` | 空 | whisper.cpp 转写服务地址（如 http://127.0.0.1:8082） |
| `CANVAS_FFMPEG_PATH` | 空 | ffmpeg 路径（留空在 PATH 查找） |
| `CANVAS_SHUTDOWN_TIMEOUT` | `10m` | 优雅关闭最长等待 |
| `ENABLE_PROVIDER_PLUGINS` | `false` | 服务商插件系统（如百炼模型发现） |
| `VITE_API_PROXY_TARGET` | `http://127.0.0.1:8080` | 前端开发代理目标 |

> 注意：后端**不读取** `.env` 文件，环境变量必须由启动 shell 或 Compose 注入。

---

## 十、常见问题排错（FAQ）

### Q1：后端启动报 `go-sqlite3 requires cgo to work`
**原因**：Windows 主机缺少 C 编译器。
**解决**：安装 MinGW-w64 gcc，或改用 Docker 部署（方式 C，容器内自带 gcc，一劳永逸）。

### Q2：`bun` 不是内部或外部命令
**解决**：安装 Bun 后**重开终端**（PATH 需要重新加载），或手动把 `%USERPROFILE%\.bun\bin` 加入 PATH。

### Q3：端口 3000 / 8080 被占用
**解决**：
- 方式 A/B：先关闭占用进程（`netstat -ano | findstr :3000` 找 PID）；
- 方式 C：在 `.env` 设置 `CANVAS_WEB_HOST_PORT` / `CANVAS_BACKEND_HOST_PORT` 换端口。

### Q4：前端页面打开但登录/保存报错
**排查顺序**：
1. 后端是否存活：访问 http://127.0.0.1:8080/api/health 应返回正常；
2. Docker 部署看日志：`docker compose -f docker-compose.local.yml logs -f backend`；
3. 开发模式确认 `VITE_API_PROXY_TARGET` 指向正确的后端地址。

### Q5：生成任务一直失败
1. 检查管理后台「渠道」配置：API 地址、密钥是否有效；
2. 检查模型是否启用、价格/额度是否充足；
3. 查看任务详情中的日志与失败原因；
4. 注意后端默认**拒绝访问本机/私网模型地址**（SSRF 防护），如需放行可信私网主机，用 `CANVAS_ALLOWED_PRIVATE_UPSTREAM_HOSTS` 按主机精确配置。

### Q6：字幕转写报"未配置本地转写服务"
**原因**：未部署 whisper.cpp。按 `docs/content/docs/backend/local-development.mdx` 部署并设置 `CANVAS_WHISPER_BASE_URL`。不需要字幕功能可忽略。

### Q7：Docker 构建很慢或超时
后端镜像已默认配置 `goproxy.cn` 国内 Go 模块代理；前端镜像 Bun 安装如遇网络问题，可配置 Docker 镜像加速器后重试。

### Q8：数据都存在哪里？如何备份？
- 开发模式（方式 A/B）：`仓库根目录\.local\project-workbench-debug\`（含 SQLite 数据库 `open_ai_canvas.db` 与上传文件）；
- Docker 模式（方式 C）：命名卷 `backend-data`（`docker volume ls` 查看）；备份可用 `docker run --rm -v open-ai-canvas_backend-data:/data -v D:\backup:/backup alpine tar czf /backup/data.tgz /data`。

---

## 十一、安全注意事项（部署到公网前必读）

1. **首次管理员注册**在受控网络完成，公网部署保持 `CANVAS_REGISTRATION_ENABLED=false`；
2. **CORS**：`CANVAS_CORS_ORIGINS` 必须设置为精确域名，公网禁用 `*`；使用 HTTPS 并正确转发代理头；
3. **端口**：后端 8080 只留在 Compose 内部网络，公网只暴露前端 3000；
4. **SSRF**：默认拒绝本机/私网上游地址；只通过 `CANVAS_ALLOWED_PRIVATE_UPSTREAM_HOSTS` 精确放行可信主机；
5. **密钥**：API Key 不出现在 URL、日志、错误上报中；`.env`、数据库、上传目录、`.settings-key` 权限要收紧；
6. 项目默认适合**个人/本地/可信环境**，未经安全加固不要直接作为公网多人服务。

---

## 十二、官方文档导航（进阶阅读）

| 文档 | 路径 |
| --- | --- |
| 快速开始 | `docs/content/docs/overview/quick-start.mdx` |
| 功能清单 | `docs/content/docs/overview/features.mdx` |
| 代码功能地图 | `docs/content/docs/backend/code-map.mdx` |
| 本地开发 | `docs/content/docs/backend/local-development.mdx` |
| HTTP API 合同 | `docs/content/docs/backend/http-api.mdx`（OpenAPI: `GET /api/openapi.yaml`） |
| 数据库结构 | `docs/content/docs/backend/backend-database.mdx` |
| 系统更新 | `docs/content/docs/backend/system-update.mdx` |
| 画布操作手册 | `docs/content/docs/canvas/canvas-node-manual.mdx` |
| 插件系统 | `docs/content/docs/plugins/plugin-system.mdx` |

**验证命令（改动后按范围运行）：**

```powershell
# 前端
cd web; bun run lint; bun run build

# 后端
cd backend; go test ./...

# 文档站
cd docs; bun run types:check
```

---

## 十三、本次部署实录（参考）

| 步骤 | 结果 |
| --- | --- |
| 1. 克隆仓库到 `d:\openai-canvas\open-ai-canvas` | 成功（main 分支，2283 个文件） |
| 2. 环境检查 | Git 2.55.0 ✓ / Go 1.26.3 ✓ / Node 25.2.1 ✓ / Bun 1.4.2（新装）✓ / Docker 29.6.2 ✓ |
| 3. 前端依赖 `bun install --frozen-lockfile` | 成功（1366 个包，约 69 秒） |
| 4. 主机直跑后端尝试 | 失败：Windows 无 gcc，go-sqlite3 需要 CGO → 切换 Docker 方案 |
| 5. 启动 Docker Desktop | 成功（守护进程 29.6.2） |
| 6. `docker compose -f docker-compose.local.yml up -d --build` | 成功（后端镜像 248MB + 前端镜像 252MB，两容器均 healthy） |
| 7. 接口验证 | `GET /api/health` → `{"status":"ok","ready":true,"schema":{"current":27,"ready":true},"checks":{"database":true}}` |
| 8. 浏览器验证 | 登录页 / 注册页均正常渲染；注册接口返回 `firstUser:true, registrationEnabled:true`（首个账号自动成为管理员） |

**当前运行状态：** 访问 http://localhost:3000 即可使用；首次注册的账号自动成为管理员（无需邮箱验证码）。

> 经验总结：Windows 主机部署的最大障碍是 go-sqlite3 的 CGO 依赖。**Docker 方式是最省心的 Windows 部署路径**——镜像内自带 gcc 编译环境，前端 nginx 托管 + API 反代开箱即用，数据落在 Docker 卷中便于备份。
